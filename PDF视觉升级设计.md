# doc_parser PDF 图片页/扫描件识别功能升级设计

## 1. 文档目标

本文档用于说明 `smartclaw/tool/file/doc_parser.py` 的 PDF 解析能力升级方案，覆盖需求背景、设计目标、实现方案、配置策略、错误处理、测试验证、兼容性与后续演进建议。

本文档基于当前仓库中的**最终落地版本**整理，包含后续交互中确认的调整项：

- 支持扫描 PDF、图片页 PDF、混合型 PDF 的页级识别
- 默认 `max_tokens` 提升到 `3000`
- 扫描页上限改为**单独配置项**
- 上限只限制**扫描/图片页数量**，不限制整份 PDF 的总页数
- 默认扫描/图片页上限提升到 `100`

---

## 2. 背景与问题

### 2.1 现状问题

升级前，`doc_parser` 对 PDF 的处理主要依赖纯文本提取链路：

1. `markitdown`
2. `pymupdf`
3. `pypdf`

该方案对“数字化文本 PDF”有效，但对以下文档效果较差或完全不可用：

- 扫描版合同
- 发票、票据、收据扫描件
- 截图拼接成的 PDF
- 手写笔记或拍照归档的 PDF
- 图文混合且部分页面无可提取文本的 PDF

典型问题是：

- 页面本质为图片时，纯文本抽取结果为空
- 工具只能返回“无法解析”，缺乏明确失败原因
- 混合型 PDF 中，文本页可解析，但扫描页内容会丢失

### 2.2 升级目标

本次升级的核心目标不是替换原有文本提取能力，而是在原链路基础上补齐“视觉识别兜底能力”，实现：

- 对扫描件、图片页进行自动识别
- 对混合型 PDF 进行页级分类与整合
- 对纯文本 PDF 保持原有优先级与行为
- 给出更清晰的错误分类与可调配置

---

## 3. 设计目标

### 3.1 功能目标

- 新增 PDF 页级视觉识别能力
- 自动区分文本页与图片页/扫描页
- 图片页渲染为 PNG 后调用多模态模型识别
- 输出结果按页码顺序合并为 Markdown
- 图片页识别结果带页码标记，便于溯源

### 3.2 非功能目标

- 保持 `doc_parser()` 函数签名不变
- 保持非 PDF 文件处理逻辑不变
- 保持原有文本提取器优先级不变
- 为大 PDF 增加成本、超时与页数保护
- 对异常场景提供可预期兜底行为

### 3.3 兼容性目标

- 旧调用方无需修改入参
- 旧环境变量 `SMARTCLAW_DOC_PARSER_PDF_VISION_MAX_PAGES` 继续兼容读取
- 新配置项优先使用 `SMARTCLAW_DOC_PARSER_PDF_VISION_MAX_SCAN_PAGES`

---

## 4. 总体方案

### 4.1 核心思路

升级后的 PDF 解析采用“两级决策”：

#### 第一层：是否需要页级视觉处理

在 `_run_extractors_detailed()` 中，先判断 PDF 是否存在需要视觉补充的页面：

- 若所有页面都满足“文本页”判定，则继续走原有纯文本提取链
- 若任一页面被判定为图片页/扫描页，则进入 `_extract_pdf_with_vision()` 进行页级整合

#### 第二层：页级分类与提取

在 `_extract_pdf_with_vision()` 中逐页处理：

- 文本页：直接保留页面文本
- 图片页/扫描页：渲染 PNG → base64 编码 → 调多模态模型 → 写回结果

### 4.2 处理流程

```text
doc_parser()
  └─ _run_extractors_detailed()
      ├─ 检测 PDF 是否加密
      ├─ 检测是否存在需要页级视觉处理的页面
      │   ├─ 否：走 markitdown -> pymupdf -> pypdf
      │   └─ 是：走 _extract_pdf_with_vision()
      └─ 返回统一的 _ExtractorOutcome
```

页级视觉流程：

```text
_extract_pdf_with_vision()
  ├─ 遍历整份 PDF 的所有页面
  ├─ 逐页分类
  │   ├─ 文本页：直接保留文本
  │   └─ 扫描/图片页：受 max_scan_pages 控制
  ├─ 图片页渲染 PNG（DPI=150）
  ├─ 多模态识别
  ├─ 失败时回退到原始文本或空
  └─ 按页码顺序合并 Markdown
```

---

## 5. 页面分类设计

### 5.1 分类规则

当前文本页判定规则为：

- `页面可提取文本长度 >= 50` → 视为文本页
- 否则 → 视为图片页/扫描页

对应常量：

- `PDF_TEXT_PAGE_THRESHOLD = 50`

### 5.2 为什么不再把“是否含图片对象”作为硬条件

早期方案曾考虑：

- `文本长度 >= 阈值 AND 页面无图像对象` → 文本页

但这会误伤大量真实文档：

- 正文页带 logo
- 合同页带签章图
- 报告页带小配图
- 页眉页脚中嵌入图片

因此最终改为：

- **只要文本足够多，就优先保留为文本页**

这样可以避免整页误判为扫描页，减少不必要的模型调用，也避免丢失可直接提取的正文内容。

---

## 6. 视觉提取设计

### 6.1 渲染策略

图片页使用 PyMuPDF 渲染：

- API：`page.get_pixmap(dpi=150)`
- 输出格式：PNG
- 编码方式：base64

### 6.2 DPI 选择

默认 `DPI=150`，原因：

- 比 72/96 DPI 更利于模型识别细小文字
- 相比更高 DPI（如 200/300），图片大小与成本更可控
- 适合作为默认平衡值

### 6.3 多模态调用方式

复用 `smartclaw/workflow/llm.py` 中的 `LLMClient`，新增 `ask_messages()` 用于发送多模态消息块。

消息结构：

```python
ChatMessage(
    role="user",
    content=[
        {"type": "text", "text": prompt},
        {"type": "image", "mimeType": "image/png", "data": image_b64},
    ],
)
```

### 6.4 模型配置与 SmartClaw 复用规则

PDF 视觉解析复用 SmartClaw 现有的模型配置和 Provider 体系，不单独实现模型客户端、鉴权或网关配置。调用链为：`doc_parser.py` → `LLMClient.ask_messages()` → SmartClaw `Provider` → 已配置的模型网关/API。

- `LLMClient` 位于 `smartclaw/workflow/llm.py`，底层继续使用 `Config`、`Provider`、`ProviderConfig`、`Provider.apply_config()` 和 `provider.chat()`。
- 未显式指定模型时，读取 SmartClaw 全局默认配置 `default_models.llm`。
- 可通过 `provider_id`、`model` 或 `provider/model` 显式指定模型。
- `SMARTCLAW_DOC_PARSER_PDF_VISION_MODEL` 和 `SMARTCLAW_DOC_PARSER_PDF_VISION_PROVIDER` 仅用于 PDF 视觉解析场景的可选覆盖；未设置时回退到全局默认模型。
- 模型的 API 地址、密钥、鉴权方式和 Provider 参数继续由 SmartClaw 统一管理。

### 6.5 视觉 Prompt 设计目标

Prompt 的目标不是泛化描述图片，而是尽量还原文档页面信息，要求模型：

- 提取文字、标题、关键数字
- 尽量还原列表、表格结构
- 使用 Markdown 输出
- 对无法辨认内容明确标注
- 避免编造不存在的信息

### 6.6 结果标记

图片页结果前统一追加页码标识：

```markdown
## 第 {page_no} 页（图片识别）
```

这样做的目的：

- 方便定位识别结果来源页
- 区分“原生文本提取”与“视觉识别结果”
- 便于后续调试、人工校验、问题反馈

---

## 7. 大 PDF 处理策略

### 7.1 当前策略

当前版本不再限制整份 PDF 总页数，而是只限制**扫描/图片页数量**。

独立配置项：

```text
SMARTCLAW_DOC_PARSER_PDF_VISION_MAX_SCAN_PAGES
```

默认值：

```text
100
```

### 7.2 策略含义

假设一份 PDF 有 300 页：

- 其中 260 页是文本页
- 其中 40 页是扫描页

则当前策略下：

- 260 个文本页仍会完整保留
- 40 个扫描页也会全部进入视觉处理（因为未超过 100）

若一份 PDF 有：

- 80 个文本页
- 130 个扫描页

则当前策略下：

- 80 个文本页仍保留
- 前 100 个扫描页进入视觉处理
- 其余 30 个扫描页跳过视觉识别

### 7.3 说明信息

当扫描页超限时，结果中会追加说明，例如：

```markdown
## 说明
已处理 100 个扫描/图片页；另有 30 个扫描/图片页因上限 100 未执行视觉识别。
```

### 7.4 为什么采用“限制扫描页而不是限制总页数”

这是后续交互中明确收敛出的设计决定，原因如下：

1. 文本页成本低，没必要被扫描页策略连带截断
2. 视觉识别开销主要集中在扫描页
3. 对超大混合 PDF 更合理
4. 能最大限度保留原始文本信息

---

## 8. 配置设计

### 8.1 当前配置项

| 配置项 | 默认值 | 说明 |
|---|---:|---|
| `SMARTCLAW_DOC_PARSER_PDF_VISION_DPI` | `150` | 图片页渲染 DPI |
| `SMARTCLAW_DOC_PARSER_PDF_VISION_MAX_SCAN_PAGES` | `100` | 最多执行视觉识别的扫描/图片页数量 |
| `SMARTCLAW_DOC_PARSER_PDF_VISION_MAX_TOKENS` | `3000` | 每页视觉识别的输出 token 上限 |
| `SMARTCLAW_DOC_PARSER_PDF_VISION_TIMEOUT_S` | `30` | 单页模型调用超时（秒） |
| `SMARTCLAW_DOC_PARSER_PDF_VISION_MODEL` | 无 | 指定视觉模型 |
| `SMARTCLAW_DOC_PARSER_PDF_VISION_PROVIDER` | 无 | 指定模型 provider |

### 8.2 兼容性配置

旧变量：

```text
SMARTCLAW_DOC_PARSER_PDF_VISION_MAX_PAGES
```

兼容规则：

- 如果设置了 `SMARTCLAW_DOC_PARSER_PDF_VISION_MAX_SCAN_PAGES`，优先使用新变量
- 否则兼容读取旧变量
- 读取旧变量时输出 warning 日志，提示迁移到新变量

### 8.3 max_tokens 调整结论

本次迭代中，`max_tokens` 默认值从保守值上调到了 `3000`。

原因：

- `1200` 对简单票据、发票、合同短页通常够用
- 但对复杂合同页、密集长文、大表格、多栏布局明显偏小
- `3000` 在“完整性”和“成本”之间更均衡

---

## 9. 错误处理与失败分类

### 9.1 当前错误分类

PDF 相关错误被收敛为以下几类：

| 错误标题 | 触发条件 |
|---|---|
| `PDF 需要密码` | 检测到 PDF 加密，需要密码 |
| `PDF 文本提取失败` | 文本提取链为空，且未进入有效视觉处理 |
| `PDF 图片页视觉识别失败` | 视觉页处理已尝试，但最终未获得可用结果 |
| `PDF 无法解析` | 文件损坏、结构非法、无法正常打开或识别 |

### 9.2 兜底策略

视觉链路中的单页异常不会直接中断整份文档处理：

- 图片渲染失败：回退到该页原始文本或空
- 模型调用超时/失败：回退到该页原始文本或空
- 单页识别为空：记录错误，继续处理后续页面

这样可避免“部分页面失败导致整份 PDF 全部失败”。

---

## 10. 向前兼容设计

### 10.1 对外接口不变

`doc_parser()` 保持原函数签名不变：

- `input_path`
- `path`
- `filePath`
- `output_path`
- `output`
- `overwrite`

### 10.2 非 PDF 不受影响

以下文件类型沿用原逻辑：

- `.docx`
- `.doc`
- `.ppt`
- `.pptx`
- `.xls`
- `.xlsx`
- `.html`
- `.htm`

### 10.3 纯文本 PDF 不受影响

若整份 PDF 不需要页级视觉补充，则继续走：

```text
markitdown -> pymupdf -> pypdf
```

不额外增加视觉调用成本。

---

## 11. 测试设计与覆盖范围

### 11.1 新增测试文件

```text
tests/tool/file/test_doc_parser_vision.py
```

### 11.2 覆盖场景

已覆盖以下关键场景：

1. **纯文本 PDF**
   - 验证不进入视觉分支

2. **纯图片 PDF / 扫描件**
   - 验证能进入视觉提取并返回带页码标记的结果

3. **混合 PDF（文本页 + 图片页）**
   - 验证文本页保留、图片页识别、结果按页合并

4. **加密 PDF**
   - 验证返回 `PDF 需要密码`

5. **文本链失败但未进入视觉有效处理**
   - 验证返回 `PDF 文本提取失败`

6. **视觉识别失败**
   - 验证返回 `PDF 图片页视觉识别失败`

7. **损坏/不可读 PDF**
   - 验证返回 `PDF 无法解析`

8. **空白 PDF**
   - 验证不抛异常

9. **超大扫描 PDF**
   - 验证扫描页上限与 token 配置生效

10. **超大混合 PDF**
    - 验证扫描页超限后，后续文本页仍被保留

11. **多模态消息结构**
    - 验证 `LLMClient.ask_messages()` 收到正确的图片块与参数

### 11.3 已执行验证

在迭代过程中，定向测试已验证通过：

```text
45 passed
```

此外已完成：

- 手工混合 PDF 路径验证
- secret scanning
- CodeQL 校验（0 alerts）

---

## 12. 日志与可观测性设计

### 12.1 已记录日志点

当前已为关键节点增加日志：

- PDF 打开失败
- 页面分类结果
- 模型调用开始
- 图片渲染失败
- 模型调用失败
- 扫描页上限命中
- 旧环境变量兼容读取
- PDF 密码检测异常
- 页级视觉检查异常

### 12.2 日志价值

这些日志可支持：

- 识别失败排查
- 成本与命中率分析
- 扫描页数量评估
- 配置迁移追踪

---

## 13. 已知限制

1. 模型识别准确率依赖扫描质量、分辨率、倾斜角度与遮挡情况
2. 非常复杂的表格可能仍存在结构丢失
3. 手写内容的稳定性弱于印刷体
4. 当前是逐页调用模型，大批量扫描件时耗时会线性增长
5. 扫描页超过配置上限时，超出部分不会继续做视觉识别
6. 文本页分类当前依赖文本长度阈值，极端情况下仍可能出现误判

---

## 14. 后续优化建议

### 14.1 处理策略优化

- 按页面复杂度动态调整 `max_tokens`
- 按批次并行处理扫描页
- 对超大 PDF 支持分段输出或增量输出

### 14.2 分类能力优化

- 增加基于图像面积占比的辅助判定
- 对 OCR 嵌入层很差的 PDF 增加更细粒度判定

### 14.3 成本与性能优化

- 记录单页处理耗时与识别成功率
- 引入缓存，避免相同页面重复识别
- 按 provider/model 能力做更精细的路由

### 14.4 产品体验优化

- 在前端或调用结果中增加“部分扫描页因上限未识别”的可见提示
- 增加配置说明入口，降低部署理解成本

---

## 15. 结论

本次升级完成后，`doc_parser` 已从“仅支持纯文本 PDF”提升为“支持纯文本 PDF + 扫描 PDF + 混合 PDF 的统一解析工具”，并具备以下关键能力：

- 页级视觉兜底
- 混合 PDF 按页合并
- 明确的错误分类
- 扫描页数量独立限流
- 更合理的默认 token 与页数策略
- 保持原接口与非 PDF 逻辑稳定

该方案在不引入本地 OCR 重依赖的前提下，显著提升了 PDF 解析覆盖率，适合作为当前阶段的工程化可交付版本。
